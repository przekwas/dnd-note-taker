import React from 'react';

interface PrivateProps {}

const Private = (props: PrivateProps) => {
    return (
        <div>
            <h1>Private</h1>
        </div>
    );
}

export default Private;