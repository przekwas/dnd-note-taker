import { componentColors, componentSizes, bgColors } from './constants';

export type ComponentColors = typeof componentColors[number];
export type ComponentSizes = typeof componentSizes[number];
export type ComponentBgColors = typeof bgColors[number];